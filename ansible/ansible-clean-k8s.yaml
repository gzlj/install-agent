---
- hosts: test
  remote_user: root
  tasks:
  - name: clean k8s
    shell: |
      echo y | kubeadm reset
      ip link delete cni0
      rm -fr /etc/kubernetes /etc/cni/* /var/lib/etcd /var/lib/cni/* /etc/cni/net.d/*
